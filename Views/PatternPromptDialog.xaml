<Window x:Class="Josha.Views.PatternPromptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Pattern"
        Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Background="{StaticResource Brush.Surface}"
        Foreground="{StaticResource Brush.OnSurface}"
        FontFamily="{DynamicResource Font.Body}">
    <Window.InputBindings>
        <KeyBinding Key="Escape" Command="ApplicationCommands.Close"/>
    </Window.InputBindings>
    <Window.CommandBindings>
        <CommandBinding Command="ApplicationCommands.Close" Executed="OnCancelCommand"/>
    </Window.CommandBindings>

    <StackPanel Margin="20">
        <TextBlock x:Name="LabelText"
                   FontSize="13"
                   Margin="0,0,0,10"/>
        <TextBox x:Name="PatternBox"
                 Style="{StaticResource ModernTextBoxStyle}"
                 MinHeight="28"
                 Text="*.*"/>
        <TextBlock Text="Globs only — * matches any string, ? matches one character."
                   FontSize="11"
                   Foreground="{StaticResource Brush.OnSurfaceMuted}"
                   Margin="0,6,0,16"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Click="OnCancel"
                    Style="{StaticResource SecondaryButtonStyle}"
                    MinWidth="90" Margin="0,0,8,0"/>
            <Button Content="OK" Click="OnOk"
                    Style="{StaticResource PrimaryButtonStyle}"
                    MinWidth="90" IsDefault="True"/>
        </StackPanel>
    </StackPanel>
</Window>
