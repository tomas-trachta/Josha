<Window x:Class="Josha.Views.OverwriteSheet"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Replace items?"
        Width="500" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Background="{StaticResource Brush.Surface}"
        Foreground="{StaticResource Brush.OnSurface}"
        FontFamily="{DynamicResource Font.Body}">
    <Window.InputBindings>
        <KeyBinding Key="Escape" Command="ApplicationCommands.Close"/>
    </Window.InputBindings>
    <Window.CommandBindings>
        <CommandBinding Command="ApplicationCommands.Close" Executed="OnCancelCommand"/>
    </Window.CommandBindings>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{Binding Headline, RelativeSource={RelativeSource AncestorType=Window}}"
                   FontSize="16" FontWeight="SemiBold"
                   TextWrapping="Wrap"
                   Margin="0,0,0,12"/>

        <TextBlock Grid.Row="1"
                   Text="{Binding ConflictListText, RelativeSource={RelativeSource AncestorType=Window}}"
                   FontSize="12"
                   Foreground="{StaticResource Brush.OnSurfaceMuted}"
                   TextWrapping="Wrap"
                   Margin="0,0,0,16"/>

        <TextBlock Grid.Row="2"
                   Text="What would you like to do?"
                   FontSize="13"
                   Margin="0,0,0,12"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Click="OnCancel"
                    Style="{StaticResource SecondaryButtonStyle}"
                    MinWidth="100" Margin="0,0,8,0"/>
            <Button Content="Skip all" Click="OnSkip"
                    Style="{StaticResource SecondaryButtonStyle}"
                    MinWidth="100" Margin="0,0,8,0"/>
            <Button Content="Replace all" Click="OnReplace"
                    Style="{StaticResource PrimaryButtonStyle}"
                    MinWidth="100" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
