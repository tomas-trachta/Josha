<Application x:Class="Josha.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Josha"
             xmlns:conv="clr-namespace:Josha.Converters"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles/Theme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="BoolToVis"/>
            <conv:StringNotEmptyToVisibilityConverter x:Key="StrNotEmptyToVis"/>
            <conv:InverseBoolToVisibilityConverter x:Key="InvBoolToVis"/>
            <conv:ResourceKeyToBrushConverter x:Key="ResKeyToBrush"/>

            <!-- Segoe UI Variable preferred (Win 11); Segoe UI fallback (Win 10). -->
            <FontFamily x:Key="Font.Display">Segoe UI Variable Display, Segoe UI</FontFamily>
            <FontFamily x:Key="Font.Body">Segoe UI Variable Text, Segoe UI</FontFamily>
            <FontFamily x:Key="Font.Small">Segoe UI Variable Small, Segoe UI</FontFamily>
        </ResourceDictionary>
    </Application.Resources>
</Application>
